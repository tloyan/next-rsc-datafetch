// 🐶 Importe la fonction getPosts qui va récuperer les posts en BDD
//import {getPosts} from '@/db/sgbd'

export async function GET() {
  // 🐶 Appelle la fonction `getPosts` pour recuperer les posts en bdd

  // 🐶 Retourne les données en json dans la reponse http
  // ⛏️ supprime [] et remplace le par la `data`
  return Response.json([])
  // 🐶 constate la presence des données ici:  http://localhost:3000/exercises/api/posts

  // 🐶 N'oublie pas les exercices bonus
}
